# Obsiddy seed units live here, numbered from 001.
# SeedHistory keys on the path relative to prisma/seeds/, so
# `framework-obsiddy/001-*` cannot collide with a host project's own seeds.
# See .context/framework/obsiddy/README.md
